<% provide(:title, @user.name) %>
<div>
  <aside id='user-badge'>
    <section>
      <!-- <%= gravatar_for @user %> -->
      <h1 id="user-page-name"><%= @user.name %></h1>
      <!-- <span><%= link_to "view my profile", @user %></span> -->
      <%= render 'shared/stats' %>
    <section>
      <%= render "follow_form" if signed_in? %>
    </section>
  </aside>
  <div id="user-feed">
    <%= render 'feed' %>
  </div>
</div>