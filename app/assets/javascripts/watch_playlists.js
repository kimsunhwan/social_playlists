/*
function playNextVideo(event) {
	if (event.data == YT.PlayerState.ENDED) {
		currentVideoId++;

		var nextVideo = document.getElementById('video' + currentVideoId);
		var nextURL = nextVideo.getElementsByTagName('P')[0].innerHTML;

		player.loadVideoById(nextURL);
	}
}

function playVideo(num) {
	currentVideoId = num;

	var url = document.getElementById('video' + num).getElementsByTagName('P')[0].innerHTML;

	player.loadVideoById(url);
}
*/